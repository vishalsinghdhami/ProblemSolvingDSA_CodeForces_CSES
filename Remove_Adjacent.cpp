#include <bits/stdc++.h>
using namespace std;

int main()
{

    int t;
    cin >> t;

    for (int i = t; i > 0; i--)
    {
        int n;
        cin >> n;
        int array[n];
        for (int j = t; j > 0; j--)
        {
            cin >> array[j];
        }
         for (int j = t; j > 0; j--)
        {
            cout<< array[j];
        }
    }

    return 0;
}